<Page>
    <StackLayout>
        <Button text="Get Current Location" tap="buttonGetLocationTap"/>
        <Button text="Get Accelerometer" tap="buttonAccelerometer"/>
        <Button text="Touch Id" tap="buttonTouchId"/>
        <Button text="Trillen" tap="buttonTrillen"/>
        <Button text="Camera" tap="buttonCamera"/>
        <Button text="Check ConnectionType" tap="ButtonConnectionType"/>
        <Button text="Check bluetooth" tap="ButtonBluetooth"/>
        <Button text="Push notification" tap="ButtonPush"/>
        <Button text="ActivityIndicator" tap="ButtonActive"/>
        
    </StackLayout>
</Page>